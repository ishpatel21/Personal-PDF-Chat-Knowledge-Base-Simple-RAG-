llama-index==0.12.*          # or latest stable in Feb 2026; pip show llama-index to check
llama-index-llms-groq
llama-index-embeddings-huggingface   # for free local embeddings
llama-index-vector-stores-chroma
chromadb==0.5.*              # or latest compatible
python-dotenv                # optional but recommended
pypdf                        # PDF parser (usually pulled in already)
einops==0.7.0                # required by nomic-embed-text model
streamlit                    # UI framework for interactive RAG app